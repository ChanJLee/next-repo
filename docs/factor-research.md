# 横截面因子研究记录（诚实版）

> 目标：找出能过**严格样本外验证**的真实预测信号。验收标准统一：滚动起点 CV / 点-时 +
> **按日期分块自助 95% CI**（吸收截面相关），CI 排除 0 才算真。绝不信单次 70/30 切分或看图。

## 一、被证伪的（别再走）

| 方向 | 结论 | 工具 |
|---|---|---|
| 单标的日线方向 + 各种 tilt | 滚动 CV 池化 skill −0.0054，CI 整段<0；最优收缩 s\*=0 → **生产 tilt 已清零，P(多)=点-时基准率** | `model:cv` |
| 跨资产 SPY/VIX 代理 | 微弱、生产清零后无意义 | 同上 |
| 利率(TLT)/板块(XLK) | 过拟合 2010s regime，样本外更差 | `featurize`(已回退) |
| 超卖反弹 /"深浅水反转" | 看图错觉：中性池 48% 接飞刀、edge 不显著 | `test-oversold` |
| 超买动量 | 科技池显著但中性池归零 = 选择性偏差 | `test-oversold` |
| 低波动因子 | 本池方向反（高波动赢=beta 假象） | `test-xs-momentum lowvol` |

## 二、成立的真信号

### 1. 12−1 月横截面动量（价量唯一 edge）
多样化 34 标的：持有 21 日 spread +0.32%（CI [+0.03,+0.61]）；持有 63 日 +0.72%（CI [+0.24,+1.22]）。
风险调整版≈裸动量（无改进）；纯大盘池里偏弱。`scripts/test-xs-momentum.ts`

### 2. 价值（账面/市值 B/M）——第一个和动量正交的真信号
SEC EDGAR **point-in-time**（filed≤d，无重述/滞后泄漏）× 价格，53 家大盘：
B/M spread **+0.51%/63d，CI [+0.11,+0.92]，P>0=99%**。便宜的跑赢贵的（价值溢价）。
注：质量（ROE、GP/A）在本大盘池**反向**（高质量=贵→跑输）；盈利收益率(E/P)不显著。

### 3. ⭐ 价值 + 动量（本研究的"工业级"落点）
`z(B/M) + z(mom)`，等权、理论驱动（Asness "Value & Momentum Everywhere"），不调参：
**spread +0.91%/63d，CI [+0.44,+1.41]，P>0=100%**，且**三个子时段全为正**：

| 段1 09–15 | 段2 15–20 | 段3 20–26 |
|---|---|---|
| +0.63% | +0.77% | +1.34% |

**为何稳健**：价值单独在 2015–2020 翻负（−0.96%，价值失落期），但动量在同期把组合托住（+0.77%）——
value 与 momentum 负相关，组合在任一失灵时仍正。这是多因子分散的核心。`scripts/test-fund-factors.ts valmom`

## 三、诚实定位
- 这是个**真实、稳健、无泄漏、理论驱动**的多因子横截面模型——工业级量化股票的地基。
- 但量级**温和**：~0.9%/63d 多空（毛）≈ 0.43%/月。作为可交易多空在成本后边际；作为**给股票排序的横截面信号 / 判断辅助**站得住。
- 刻意不纳入样本内挑出的"做空质量"（+1.16% 但数据窥探，不诚实）。

## 四、数据/工具
- `src/lib/data/edgar.ts`：EDGAR PIT 客户端（`pitFact` / `pitAnnualFlow`）。SEC_USER_AGENT 环境变量设联系邮箱。
- `scripts/featurize-fundamentals.ts` → `data/fund-cache.json`（gitignore）；`scripts/test-fund-factors.ts` 测单因子/组合。
- 复现：`pnpm exec tsx scripts/featurize-fundamentals.ts 500 10 63` 然后 `... test-fund-factors.ts fund-cache.json valmom`。

## 五、下一步（若再推进）
- **产品化**：把 value+momentum 百分位做成 app 里的横截面信号/徽章（需运行时缓存 EDGAR 基本面 + 固定参考池）。
- **更强**：扩大universe（含中小盘，动量更强）；加正交因子需更谨慎防数据窥探（用真 OOS、预先指定权重）。
